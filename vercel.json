{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile", 
  "buildCommand": "npm run build", 
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/.*",
      "dest": "/index.html" 
    }
  ]
}
